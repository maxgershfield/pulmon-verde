## Pulmón Verde Website (static)

This folder is a static export/snapshot of the Pulmón Verde website, intended to be hosted as a static site (e.g. on Vercel).

### Deploy on Vercel

- Framework preset: **Other**
- Build command: **(none)**
- Output directory: **.**

Vercel will serve `index.html` at the root.

